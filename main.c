// WAP to add two numbers
#include <stdio.h>
int main(){
int x,y,add = 0;
printf("Enter the value of x and y");
scanf("%d %d",&x,&y);
add = x + y;
printf("the sum of x and y is: %d",add);

    return 0;
}